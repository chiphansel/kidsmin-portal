# copy to .env.development or .env.production and fill in
APP_ENV=development
PORT=3000
DB_HOST=localhost
DB_USER=dev_user
DB_PASS=dev_pass
DB_NAME=kidsmin_dev
JWT_SECRET=change_me
FRONTEND_URL=http://localhost:4200
CORS_ORIGIN=http://localhost:4200

# SMTP for invite emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=username
SMTP_PASS=password
MAIL_FROM="KidsMin Portal <no-reply@example.com>"
